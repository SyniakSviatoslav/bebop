# bebop2 Math / Spectral Verification — V2

- Audit ID: `proc_5c7f2de32260` (claude -p, ADVERSARIAL MATH/SPECTRAL VERIFICATION)
- Target: `bebop2/core/src/` math modules (field, vsa, algebra, kalman, lyapunov, chebyshev, fft, active)
- Oracle: `/root/bebop-repo/rust-core/src/lib.rs` (old = ground truth)
- Date: 2026-07-10
- Verdict: **CONDITIONAL FAIL** — crate did not compile for either target at audit time; several
  modules violate ARCHITECTURE.md "no dense / spectral-native" directives; one broken-invariant
  false-claim (fexp). `V2_EXIT=0`.

## Headline
- `cargo build --target wasm32-unknown-unknown` → **84 errors** (field 29, kalman 26, fft 9,
  chebyshev 8, lyapunov 3, active 3, algebra 2). Root causes: `Vec`/`vec!` without `alloc::`
  imports despite `extern crate alloc;`; `.sin()/.cos()/.ln()` not on `core::f64`; no
  `#[panic_handler]` / `#[global_allocator]`. Falsifies "compiles to wasm, empty import section."
- Native `cargo test` → 1 error at `pq_kem.rs:874` (out of scope for math, but one crate → blocked
  all math tests). **RESOLVED post-audit:** pq_kem was converted to coefficient-domain schoolbook
  multiplication; `cargo test -p bebop2-core --lib` now passes 54/54.

## Findings (filed by auditor; severities preserved)

| # | Sev | File:line | Issue |
|---|-----|-----------|-------|
| F1 | HIGH | crate-wide | wasm32 doesn't compile (84 errors) — see headline |
| F2 | HIGH | pq_kem.rs:874 | Blocked native `cargo test` entirely (RESOLVED post-audit) |
| F3 | HIGH | chebyshev.rs:42-79 vs lib.rs:43-76 | **Three fexp copies.** lib.rs has the real C8 fix (well-tested x<0) but it's unused dead code. chebyshev.rs re-implements the OLD BROKEN `fround=ftrunc(x+0.5)` bit-for-bit, and field.rs imports THAT on the production spectral path. Its test claims "C8 already fixed" but only checks fexp(0)/fexp(1), no negative. **RESOLVED post-audit:** chebyshev::fexp now delegates to crate::fexp (C8-correct); `fround` made symmetric; the 4 mass-conservation tests then surfaced a latent `1u64 << k` overflow (panics for k>=64) in crate::fexp's `two_k` term — fixed root-cause with `2.0f64.powi(k)`. fexp x<0 + symmetry RED tests added; full suite 54/54 green. |
| F4 | HIGH | kalman.rs:25-81, lyapunov.rs:18-60 | Both reuse the Jacobi eigensolver (symmetric-only) for general matrices, hardcode Complex imag=0.0 — asymmetric/oscillatory eigenvalues can never be represented. Tests only exercise symmetric/diagonal. Also not sqrt/PSD-preserving. (F4b MED: `kalman::invert` has no pivot-zero guard → NaN/Inf on singular V.) |
| F5 | MED-HIGH | field.rs:45-111 | `LaplacianSpectrum::from_edges` builds dense n×n + Jacobi O(n³), no size guard — the "naive dense where Lanczos/Krylov is demanded" pattern; load-bearing for active.rs free-energy. The production propagator (`chebyshev::spectral_propagate`) is correctly matrix-free. |
| F6 | MED | vsa.rs:28-29,45-46,74-75 | `bind`/`unbind` doc says scratch "must be ≥ padded" but runs FFT over the whole passed slice — an oversized power-of-two scratch silently changes convolution length, wrong answer. Never tested (tests always pass exact-size scratch). |
| F7 | MED | field.rs:181, active.rs:95 | "B11 dt corridor" guard only fires on `dt<=0.0`, doesn't catch the historically-bad `dt=0.05`, no real CFL bound. Reg test uses a low-degree path graph where 0.05 is stable anyway. Real B11/C2 bugs live in `crates/bebop` (`change_impact`/`permit_action`), not here — "carry-forward" label implies a fix that isn't happening. |
| F8 | LOW-MED | field.rs:191-196 | "C2 saturate-then-gate" correct for what it does, but ±1e6 only slows (doesn't stop) divergence; under instability stops pruning almost everything. Same label-vs-fix gap as F7. |

## Clean / matches oracle
`algebra::sinc`, `cosine_similarity` zero-guard, `vsa::unbind` `mag2>1e-30` guard, `chebyshev`
`1/lambda_max` safety — faithfully ported, no NaN/div-by-zero gaps beyond F4b. field/chebyshev/
algebra test graphs + tolerances are honest ports of old rust-core inputs. kalman/lyapunov/fft/
vsa bind-unbind are new (no old-rust-core counterpart) — self-verified vs brute-force/DFT oracles,
fine in principle.

## Resolution status (post-audit, 2026-07-10)
- **F2 — RESOLVED**: pq_kem converted to coefficient-domain; `cargo test -p bebop2-core --lib` → 54/54.
- **F1 — OPEN** (corroborates V3 #1): wasm build needs alloc imports + no_std f64 trig shims +
  panic_handler/global_allocator before the empty-import claim holds.
- **F3 — RESOLVED (2026-07-10 02:2x)**: chebyshev.rs::fexp now delegates to crate::fexp; `fround` made
  symmetric; lib.rs `two_k` overflow (`1u64 << k` for k>=64) fixed → `2.0f64.powi(k)`. Added fexp
  x<0 + symmetry RED tests; the 4 previously-failing mass-conservation tests now pass. 54/54 green.
- **F4, F5 — OPEN** (corroborate V3 #3/#4): dense eigensolver + no sqrt-Kalman; need Lanczos/Krylov
  + Potter-Carlson before "better-math-per-function" claim holds.
- **F6, F7, F8 — OPEN**: vsa scratch length, B11/C2 label-vs-fix gaps.

Note: V2 ran against the BROKEN pq_kem state (pre-schoolbook-fix), so its F2 "zero tests ran" is
stale; the math-module tests themselves are unaffected by that fix and pass under the current 54/54.
